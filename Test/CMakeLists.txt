find_library(MECAPPLIB NAMES mecapp libmecapp PATHS ../)
if ( MECAPPLIB )
  add_executable(illumination.out src/illumination.cpp)
  target_link_libraries(illumination.out ${LIB} ${MECAPPLIB})
else()
    message( STATUS "libmecapp.so was not found!" )
endif()
